#!/bin/bash

echo
echo "your dir : $PWD"

rm -rf cscope*
rm -rf *TAGS*



# c
find . -name "[^.]?*.[ch]" -print0 | xargs -0 etags

# c++
find . \( -name "[^.]?*.h" -o -name "[^.]?*.cpp" \) -print0 | xargs -0 etags
